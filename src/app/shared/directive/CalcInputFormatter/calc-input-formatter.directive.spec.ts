import { CalcInputFormatterDirective } from './calc-input-formatter.directive';

describe('CalcInputFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new CalcInputFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
